%p#notice= notice

.card.w-75.mb-3.mt-5
  .card-body
    %h5.card-title= @category.name
    %p.card-text= @category.description
    = link_to "Back to List", categories_path, class: "btn btn-primary"
    = link_to "Edit", edit_category_path(@category), class: "btn btn-warning" if user_signed_in?